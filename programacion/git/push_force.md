```bash
git push --force-with-lease
```

Hacemos force pero solo si tenemos en local lo último del repo remoto.

Evita que podamos pisar un código que se ha subido al remoto despues que hayamos hecho fetch.
