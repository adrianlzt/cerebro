Validación de JSONs con un schema definido también en JSON.

Validador online:
<https://www.jsonschemavalidator.net/>

Generar un json schema a partir de un fichero json:

```bash
uvx genson dev.json > dev.schema.json
```
