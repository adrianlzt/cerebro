Si usamos scripts en python (u otros donde declaramos shebang), necesitaremos darle permisos de ejecución sobre el ejecutable.

Parece que esto se suele hacer con:
```
corecmd_exec_bin(testprog_t)
```
