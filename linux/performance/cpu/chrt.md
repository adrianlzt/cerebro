chrt            # modifica scheduling class
  -b              # batch
  -f              # fifo
  -i              # idle
  -o              # other
  -r              # round-robin

chrt [opts] PID


Mirar planificador.md
