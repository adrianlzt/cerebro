sudo apt-get install vino
vino-preferences


# sunshine

sunshine + moonlight (ios)
sunshine usar el appimage
abir https://localhost:47990/welcome

Permitir acceso
```bash
sudo firewall-cmd --add-port={47984,47989,48010}/tcp
sudo firewall-cmd --add-port={47998,47999,48000,48002,48010}/udp
```

Buscar en el ipad, me salta una notificación para meter un pin en el host.
