HAR (HTTP Archive) is a file format used by several HTTP session tools to export the captured data.

Los navegadores nos permiten bajar una sesión en formato .har.
Chrome nos permite también cargar en el navegador un .har externo.

Podemos subir el .har a una web para ver el resultado: https://toolbox.googleapps.com/apps/har_analyzer/
