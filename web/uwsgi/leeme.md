Interfaz entre servidores web y aplicaciones.

Lo uso como conector entre nginx y django (graphite-web).

Logs: /var/log/uwsgi/app

Hace falta el plugin de python para poder usarlo con django

Socket por defecto: /run/uwsgi/app/graphite/socket
