/var/lib/origin/openshift.local.volumes/pods
Directorio donde encontramos un directorio por cada pod con su UUID.
Dentro encontramos los ficheros que se montan, por ejemplo el /etc/hosts
Si tiene algún confiMap también estará en este directorio.
