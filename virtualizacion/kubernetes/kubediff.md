https://github.com/weaveworks/kubediff

Kubediff: a tool for Kubernetes to show differences between running state and version controlled configuration.

Muestra diferencias entre, por ejemplo, los yaml de un repo de git y lo desplegado.
Envía los datos a prometheus.
Podemos poner alarmas si hay diferencias.


Parece una pieza de flux, el que realiza despliegues en k8s cuando detecta cambios en un repo.
