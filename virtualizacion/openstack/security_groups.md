Son reglas de filtrado que se ponen delante de cada VM.
Tras modificarlas en horizon tarda unos segundos (~10s) en aplicarse.

Si tenemos un sec.group, arrancamos una máquina con él, y luego modificamos el sec.group. El cambio SI se aplica en las máquinas.
