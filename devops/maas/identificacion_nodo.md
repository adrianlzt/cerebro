Cuando el nodo carga por PXE, pserv hace una petición a la API con la MAC del nodo.
Esta MAC se consulta en la base de datos (maasserver_macaddress) para ver si corresponde a algún nodo (maasserver_node)

