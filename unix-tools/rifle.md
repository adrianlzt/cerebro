Para abrir cualquier tipo de fichero sin preocuparnos por el tipo.
El se encargará de elegir con que abrirlo:

sudo apt-get install ranger

rifle fichero.jpg
rigle fichero.mp3

echo "alias open=rifle" >> ~/.bash_aliases
source ~/.bash_aliases
