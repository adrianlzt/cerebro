getent - get entries from Name Service Switch libraries

getent passwd usuario
  nos devuelve la entrada del usuario en el /etc/passwd

getent group pepe
  nos devuelve la entrada de pepe en /etc/group
