Octal dump:

Se le pueden pasar parámetros para sacar el dump en octal, ascii, hexadecimal, combinado, etc


Lo más parecido a convertir un texto binario a una ristra hexadecimal ordenada correctamente:
od -A x -t x1 -w200
