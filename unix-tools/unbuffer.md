Truco para que el redirect tenga los colores, que el comando habrá quitado por detectar que envia a una pipe.

unbuffer ansible-playbook playbook.yml | tee ansible.log

Se instala con el package: expect
