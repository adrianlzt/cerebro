Para almacenar certificados, keys y secrets.

# CLI

```bash
az keyvault secret list --vault-name NOMBRE-VAULT
```

Leer un secret:
```bash
az keyvault secret show --vault-name VAULT --name SECRETO
```
